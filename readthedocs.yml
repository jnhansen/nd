version: 2

sphinx:
  configuration: doc/conf.py

formats: all

build:
  image: latest

conda:
  environment: doc/environment.yml

python:
  version: 3.7
  install:
    - method: pip
    - path: .
    - requirements: doc/requirements.txt
